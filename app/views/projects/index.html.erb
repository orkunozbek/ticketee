<h3>Welcome</h3>

<%= link_to "New Project", new_project_path %>

<% @projects.each do |project| %>
	<li><%= link_to project.name, project %> | <%= link_to "edit", edit_project_path(project) %></li>
<% end %>